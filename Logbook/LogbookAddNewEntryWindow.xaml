<Window x:Class="Logbook.LogbookAddNewEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Logbook"
        mc:Ignorable="d"
        Title="Add New Entry to the Logbook" Height="450" Width="800" Topmost="True" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <Button x:Name="ButtonClose" Grid.Row="1" HorizontalAlignment="Right" Margin="0,0,15,0" Content="Close" Height="22" Width="75" Click="ButtonClose_Click"></Button>
        
    </Grid>
</Window>
