<Window x:Class="Logbook.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Logbook"
        mc:Ignorable="d"
        Title="About" Height="450" Width="400" WindowStartupLocation="CenterScreen" ShowInTaskbar="False" ResizeMode="NoResize" Topmost="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>

        <Rectangle Fill="#FF324049" Stroke="Black"/>

        <Label x:Name="Copyright" Content="Copyright © Bartosz Boleslaw Jakoktochce" HorizontalAlignment="Center" Height="27" Margin="0" VerticalAlignment="Center" Width="237" Grid.Row="1"/>
        <Label Content="Logbook" HorizontalAlignment="Center" Margin="0" VerticalAlignment="Center" FontSize="48" Panel.ZIndex="1" Foreground="White"/>

        <Button Content="OK" HorizontalAlignment="Center" Margin="0" VerticalAlignment="Center" Width="75" Click="OKButton_Click" Grid.Row="2"/>

    </Grid>
</Window>
